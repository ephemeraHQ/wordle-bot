{
  "name": "wordle-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w & sleep 1 && nodemon --quiet dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@redis/client": "^1.6.0",
    "@xmtp/message-kit": "1.1.7-beta.12",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11"
  },
  "engines": {
    "node": ">=20"
  }
}
